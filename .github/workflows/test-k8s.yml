name: K8s Connectivity Test
on: 
  workflow_dispatch: # Allows manual triggering from GitHub UI

jobs:
  check-cluster:
    runs-on: k8s-worker1  # This is the "magic" line
    steps:
      - name: Hello World
        run: echo "The runner is alive on $(hostname)"

      - name: Check K8s Access
        run: kubectl get nodes