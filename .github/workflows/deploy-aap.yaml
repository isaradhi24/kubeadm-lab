name: CI Pipeline
on: [push]
jobs:
  build:
    runs-on: [self-hosted, linux, x64]
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      
      - name: Deploy to Cluster
        run: | 
           kubectl apply -f apps/app-test/app-test.yml
           kubectl get pods -n default  