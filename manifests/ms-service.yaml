apiVersion: v1
kind: Service
metadata:
  name: ms1-service
  namespace: dev-ns
spec:
  type: NodePort
  selector:
    app: ms1
  ports:
    - port: 80
      targetPort: 8080 # The Magic Number
      nodePort: 30009